# Copy to .env for local docker-compose testing (never commit the real
# .env -- it's gitignored). The EC2 deployment will supply these as real
# environment variables on the instance instead of a file.

NEO4J_PASSWORD=localtestpassword
OPENROUTER_API_KEY=
LANGCHAIN_TRACING_V2=false
LANGCHAIN_API_KEY=
