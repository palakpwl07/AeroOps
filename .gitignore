# Secrets
.streamlit/secrets.toml
**/.streamlit/secrets.toml
.env
**/secrets.toml

# Virtual environment
Include/
Lib/
Scripts/
etc/
share/
pyvenv.cfg
pyvenv
__pycache__/
*.pyc

# Local dev artifacts
chroma_db_v4/
ragas_outputs/
redundant_code/
Evaluations/
Resources/

# Archived naive RAG + agentic router (superseded by GraphRAG-only
# architecture, kept locally as a private reference, not the shipped
# architecture -- see archive_naive_agentic/README.md)
archive_naive_agentic/
archive_naive_agentic.zip

# Eval / notebook files (not needed for deployment)
*.xlsx
*.ipynb

# Exception: the evaluation/ folder holds the final, citation-linked eval
# deliverables (gold set, RAGAS inputs/outputs, mechanical scores) -- keep
# its xlsx files even though *.xlsx is excluded everywhere else.
!evaluation/**/*.xlsx
aeroops v1
AeroOps_Copilot_Build_Spec
RAGAS vs GRAPH RAG
MaintenancePilot v1
requirements-PalaksLP
requirements
run_eval.py
tests.py
Ragas_eval*.py
# Experiment / tool output (untracked 2026-09; kept locally, not project content)
graphify-out/
mech_scoring_orig/
mech_scoring_trimmed/
mech_fresh_cap10/
mech_fresh_cap16/
graphrag_cap10_ragas_inputs.json
graphrag_cap16_ragas_inputs.json
graphrag_trimmed_ragas_inputs.json
agentic_rag_ragas_inputs.json
